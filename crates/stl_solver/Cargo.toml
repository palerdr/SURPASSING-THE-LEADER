[package]
name = "stl_solver_rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "stl_solver_rs"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "*", features = ["extension-module"] }
numpy = "*"
ndarray = "*"
rand = "0.8"
