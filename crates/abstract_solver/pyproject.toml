[build-system]
requires = ["maturin>=1.14,<2"]
build-backend = "maturin"

[project]
name = "abstract-solver-rs"
version = "0.1.0"
requires-python = ">=3.12,<3.14"

[tool.maturin]
features = ["extension-module"]
