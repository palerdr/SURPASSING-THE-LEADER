# Rust Workspace Instructions

- Rust kernels are opt-in accelerators behind Python parity tests.
- `stl_solver` may mirror only explicitly covered STL engine/solver behavior.
- Do not make Python runtime behavior depend on an unchecked Rust path.
- Keep build and parity guidance in `crates/docs/`.
- Run `cargo test --workspace` after Rust or binding changes.

