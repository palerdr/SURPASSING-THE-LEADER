# Probe Scripts

These scripts are exploratory probes that helped characterize tablebase and
pre-leap feasibility. They are not production entrypoints; keep new repeatable
workflows in `scripts/run_*` or `scripts/compare_*`.
