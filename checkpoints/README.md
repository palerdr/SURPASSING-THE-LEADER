# `checkpoints/`

Generated solver, corpus, tablebase, and value-net artifacts.

`scripts/run_tier_a.py` writes Tier A outputs to:

```text
checkpoints/tablebase/tier_a/
```

Most contents here are generated and can be large. Do not treat this directory
as source code.
