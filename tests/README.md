# Tests

The pytest suite covers engine mechanics, exact solver invariants, training
gates, checkpoint comparison tools, and CLI plumbing. The firewall tests are
non-negotiable for solver-core changes.
