\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{microtype}
\usepackage{parskip}
\usepackage[font=small,labelfont=bf]{caption}
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{colormaps}
\usetikzlibrary{arrows.meta}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  citecolor=blue,
  urlcolor=blue
}
\setlength{\emergencystretch}{3em}
\allowdisplaybreaks

\definecolor{figblue}{HTML}{2A6FB0}
\definecolor{figorange}{HTML}{D1622B}
\definecolor{figteal}{HTML}{1D9E75}
\definecolor{figpurple}{HTML}{534AB7}
\definecolor{figink}{HTML}{242424}
\definecolor{figboxgray}{HTML}{A0A0A0}
\definecolor{figlightgray}{HTML}{E8E8E8}
\definecolor{figpaleteal}{HTML}{E8F4F0}
\definecolor{figpalepurple}{HTML}{F1EEFB}

\pgfplotsset{
  every axis/.append style={
    line width=0.5pt,
    axis line style={black!70},
    tick style={line width=0.4pt, black!60},
    label style={font=\small},
    tick label style={font=\footnotesize},
    legend style={font=\footnotesize, draw=none, fill=none},
  },
}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\states}{\mathcal{X}}
\newcommand{\expect}{\mathbb{E}}
\newcommand{\val}{\operatorname{val}}
\newcommand{\gap}{\operatorname{gap}}
\newcommand{\alive}{\operatorname{alive}}
\newcommand{\tol}{\tau_0}

\title{An Exact Solution to the\\Drop-the-Handkerchief Timing Game}
\author{James Cenawood\thanks{Code and instructions to reproduce the results
are available in the project repository.}}
\date{July 31, 2026}

\begin{document}
\maketitle

\begin{abstract}
Drop the Handkerchief (DTH) is a fully observed, two-player, zero-sum
stochastic game with simultaneous exact-second actions, a hard 300-second
capacity, and a probabilistic revival rule. We solve the complete game. Once
a player can no longer survive an injection, the game never reads their
cumulative time dead again. Quotienting by that observation collapses
$5.27\times10^{9}$ reachable states to $289{,}374{,}121$ equivalence
classes, a factor of $18.2$. A potential function organizes those classes
into $1{,}201$ layers and permits a single backward sweep without search or
reachability analysis. Every stored value is accepted only with an explicit
equilibrium certificate whose saddle gap against the full $60\times60$ stage
matrix is at most $10^{-6}$. A ladder of certified solvers produced these
values; $99.8\%$ of the equilibria had full support. The production sweep ran
once through a parallel Rust kernel, while a Python reference matched it byte
for byte on every region computed by both. The certified root value is
\[
V(0,0,0,0)=0.0898500728095116,
\]
so under optimal play the first Dropper wins with probability
$\approx0.5449$. The computation took $5{,}432$ seconds, or about $1.51$
hours, on one twelve-core desktop, against a previously recorded
single-core projection of $3.7$--$5.1$ years.
\end{abstract}

\section{Introduction}

DTH is the pure timing core of the ``Surpassing the Leader'' wager from the
manga \emph{Usogui}. Two
players alternate the roles of \emph{Dropper} and \emph{Checker}. Each round
both simultaneously commit to a second in $\{1,\dots,60\}$; the check
succeeds if it does not precede the drop, in which case the checker absorbs
the inclusive elapsed interval, also called squandered time (ST), as poison
in a personal 300-second cylinder, and the roles swap. A failed check injects
the checker with their accumulated load
plus a fixed 60-second penalty; the injection is fatal unless a frozen,
monotone revival law (Figure~\ref{fig:revival}) saves them, in which case
their cylinder resets and their cumulative time dead (TTD) grows. Overflowing
the cylinder is fatal outright. The last player alive wins. That is the
entire game.

The game is finite but large, stochastic, and simultaneous. Its
stage games require mixed strategies, and its state space was, until now,
handled by bounded search around certified anchor values. This paper records the
complete solution and the mathematics that made it cheap:

\begin{enumerate}[leftmargin=*]
\item a \emph{per-player quotient} (Section~\ref{sec:quotient}): death
  eligibility is absorbing and zeroes the probability model, the only expression that ever reads a
  player's time dead, so that coordinate collapses to a sentinel
  independently for each player;
\item a \emph{potential} (Section~\ref{sec:potential}) that strictly
  increases along every live transition, so a single descending sweep sees
  every child before its parents, thus eliminating search, reachability
  analysis, and frontier bookkeeping in one stroke;
\item a \emph{certificate discipline} (Section~\ref{sec:certified}): no
  value enters the table without a mixed-strategy pair whose one-step
  exploitability against the fully reconstructed stage matrix is at most
  $\tol=10^{-6}$, no matter which solver produced it;
\item the $61$-class \emph{Toeplitz structure} of the stage matrices
  (Section~\ref{sec:stage}), which makes certificates and pure-saddle tests
  $O(60)$ or $O(60k)$ instead of $O(60^2)$.
\end{enumerate}

Section~\ref{sec:computation} reports the computation, its
cross-implementation audit, and the error analysis. Section~\ref{sec:claims}
states what is and is not established.

\section{The game}\label{sec:game}

\begin{definition}[State]\label{def:state}
A live state is $x=(s_c,t_c,s_d,t_d)\in\states$, written from the
perspective of the current \emph{Dropper}: $s_c\in\{0,\dots,299\}$ and
$t_c\in\{0,\dots,300\}$ are the current \emph{Checker's} accumulated load
(``squandered time'', ST) and cumulative time dead (TTD), and
$(s_d,t_d)$ are the Dropper's. Terminal payoffs are $+1$ when the current
Dropper wins and $-1$ when they lose.
\end{definition}

\begin{definition}[Actions and timing]\label{def:actions}
Both players simultaneously choose seconds $d,c\in\{1,\dots,60\}$ (drop and
check). The check \emph{succeeds} iff $d\le c$, and then the checker's load
grows by the inclusive interval $\ell=c-d+1\in\{1,\dots,60\}$; equal seconds
cost one second, not zero. If $s_c+\ell\ge300$ the cylinder overflows and
the checker dies: the transition ends with payoff $+1$ for the Dropper.
Otherwise play continues from
\[
(s_c,t_c,s_d,t_d)\ \xrightarrow{\ \text{success, lag }\ell\ }\
(s_d,\,t_d,\,s_c+\ell,\,t_c),
\]
with the roles swapped (the previous Checker drops next).
\end{definition}

\begin{definition}[Failure, dose, and revival]\label{def:failure}
On a failed check ($d>c$) the checker is injected with the dose
$q=s_c+60$. Survival is possible iff
\begin{equation}\label{eq:survive}
q<300\ \wedge\ q+t_c\le300
\qquad\Longleftrightarrow\qquad
s_c\le239\ \wedge\ s_c+t_c\le240,
\end{equation}
where the second inequality is deliberately non-strict: a resulting
cumulative TTD of exactly $300$ remains eligible and thus survivable. When
\eqref{eq:survive} holds, the checker revives with the frozen probability
\begin{equation}\label{eq:revival}
p(s_c,t_c)\;=\;0.95\left(1-\frac{s_c}{240}\right)0.75^{\,t_c/60},
\end{equation}
their cylinder resets, and their TTD grows by the dose:
\[
(s_c,t_c,s_d,t_d)\ \xrightarrow{\ \text{failure, revive}\ }\
(s_d,\,t_d,\,0,\,t_c+s_c+60)
\quad\text{with probability }p(s_c,t_c),
\]
else the transition ends with payoff $+1$. When \eqref{eq:survive} fails, the
injection is fatal with certainty and $p\equiv0$.
\end{definition}

The source gives no numerical revival odds, but it fixes the qualitative
geometry: a failed check injects the vial contents plus sixty seconds against a
five-minute cap; nearby deaths lower revival prospects enough to change
strategy; and the referee's condition separately affects
resuscitation~\cite{stlevidence}. These facts motivate the separable form in
\eqref{eq:revival}. Since $q=s+60$, the acute factor
$1-s/240=(300-q)/240$ is the simplest monotone path from a bare injection to
the lethal dose, so each second of ST consumes an equal share of acute
headroom. TTD records damage carried through earlier revivals;
$0.75^{t/60}$ makes each additional death-minute retain a fixed fraction of
the chance remaining after the acute dose. The source supports the signs and
endpoint, not linearity, multiplicative separation, or the constants $0.95$
and $0.75$; those are frozen modeling choices beneath every result below.

Values are always from the current Dropper's perspective and negate across
the role swap; the game is zero-sum with terminal-only payoff in
$\{-1,+1\}$, so all values lie in $[-1,1]$. We call the checker profile
$(s,t)$ \emph{dead} when \eqref{eq:survive} fails --- the player is still
playing, but any future failed check is certainly fatal for them.
Figure~\ref{fig:revival} plots the law. It is smooth and positive on the alive
triangle $s+t\le240$, then identically zero outside it. TTD is read nowhere
else in the rules, so Section~\ref{sec:quotient} collapses the zero region.

\begin{figure}[t]
\centering
\includegraphics[width=0.95\textwidth]{build/figures/seaborn/fig1_revival_probability.pdf}
\caption{The frozen revival law \eqref{eq:revival}. Color and height both
encode probability. The blue edge isolates the linear loss from load; the
purple edge isolates the exponential loss from time dead. The orange curtain
marks $s+t=240$: probability is positive on the curtain and drops to zero
immediately beyond it.}
\label{fig:revival}
\end{figure}

\section{Finiteness and stage structure}\label{sec:stage}

\begin{lemma}[Damage strictly increases]\label{lem:damage}
Let $R(x)=s_c+t_c+s_d+t_d$. Every live transition strictly increases $R$:
a successful lag $\ell$ adds $\ell\ge1$, and a revived failure adds exactly
$60$ (the dose $s_c+60$ enters TTD while the load $s_c$ resets).
\end{lemma}

\begin{corollary}[The game is finite]\label{cor:finite}
$R\le299+300+299+300=1198$ on live states, so every play reaches a terminal
outcome within $1198$ live transitions. The game therefore has a unique
value $V:\states\to[-1,1]$, obtained by backward induction over the finite
acyclic transition graph~\cite{zermelo1913}, solving at each state the
zero-sum simultaneous stage game by von Neumann's minimax
theorem~\cite{vonneumann1928}; the construction is the finite, absorbing
case of a Shapley stochastic game~\cite{shapley1953}.
\end{corollary}

\begin{proposition}[The 61 transition classes]\label{prop:classes}
Fix a live state $x$ and let $V$ assign values to its children. Define
\begin{align*}
S_\ell&=
\begin{cases}
+1,& s_c+\ell\ge300,\\
-V(s_d,t_d,s_c+\ell,t_c),&\text{else},
\end{cases}\\[3pt]
F&=
\begin{cases}
+1,& (s_c,t_c)\ \text{dead},\\
p\,\bigl(-V(s_d,t_d,0,t_c+s_c+60)\bigr)+(1-p),&\text{else}.
\end{cases}
\end{align*}
Then the $60\times60$ stage matrix of $x$, with the Dropper as row player,
is
\begin{equation}\label{eq:toeplitz}
M(x)_{d,c}=
\begin{cases}
S_{c-d+1}, & c\ge d,\\
F, & c<d,
\end{cases}
\end{equation}
and $V(x)=\val(M(x))$. In particular $M(x)$ carries only $61$ distinct
entries: it is Toeplitz on and above the diagonal and constant below it.
\end{proposition}

\begin{proof}
A successful cell depends on the actions only through the lag
$\ell=c-d+1$, and every failed cell has the identical transition
distribution; the entries are the expected continuation values, negated for
the role swap. The Bellman equation is Corollary~\ref{cor:finite}.
\end{proof}

\begin{lemma}[$O(60)$ saddle reductions]\label{lem:toeplitz}
Row $d$ of \eqref{eq:toeplitz} contains exactly the prefix
$S_1,\dots,S_{61-d}$ together with $F$ when $d>1$, and column $c$ contains
the prefix $S_1,\dots,S_{c}$ together with $F$ when $c<60$. Hence with
prefix scans $m_j=\min_{i\le j}S_i$ and $M_j=\max_{i\le j}S_i$,
\begin{align*}
\max_d\min_c M(x)_{d,c}
&=\max_d
\begin{cases}
m_{60}, & d=1,\\
\min(m_{61-d},F), & d>1,
\end{cases}\\[3pt]
\min_c\max_d M(x)_{d,c}
&=\min_c
\begin{cases}
\max(M_{c},F), & c<60,\\
M_{60}, & c=60,
\end{cases}
\end{align*}
computable in $O(60)$ operations, exactly (min and max are associative and
commutative on floats), from the $61$ class values alone.
\end{lemma}

Figure~\ref{fig:toeplitz} draws the layout. Each row contains a prefix of the
$S$ array, never an arbitrary subset. Two linear scans therefore reproduce
every row minimum and column maximum without materializing the matrix.

\begin{figure}[t]
\centering
\includegraphics[width=0.98\textwidth]{build/figures/seaborn/fig2_toeplitz_structure.pdf}
\caption{An $8\times8$ view of the stage matrix \eqref{eq:toeplitz}. The lag
$\ell=c-d+1$ is constant on diagonals, while every failed check has value
$F$. Row minima and column maxima reduce to prefix scans
(Lemma~\ref{lem:toeplitz}), giving the same result as a scan of all
$3{,}600$ cells.}
\label{fig:toeplitz}
\end{figure}

\section{The per-player dead-TTD quotient}\label{sec:quotient}

The complete game reads a player's TTD in exactly one expression: the
revival probability \eqref{eq:revival} of a failed check. That single observation
powers the entire reduction.

\begin{lemma}[Death is absorbing]\label{lem:absorbing}
If the profile $(s,t)$ is dead, then $(s',t)$ is dead for every
$s'\ge s$. Since a successful check raises a player's load and leaves their
TTD unchanged, and a failed check either ends the game for them or requires
\eqref{eq:survive}, load growth is the only motion available to a dead
profile, and it never revives it.
\end{lemma}

\begin{theorem}[Quotient soundness]\label{thm:quotient}
Define $\varphi(s,t)=(s,t)$ when $(s,t)$ is alive and
$\varphi(s,t)=(s,\bot)$ otherwise, where $\bot$ is the dead-TTD sentinel at
load $s$. Apply this map to each player's profile independently. Then any two
live states with equal images under $\varphi$
have identical stage matrices up to $\varphi$-identification of their
children, and hence identical values: $V$ factors through $\varphi$.
\end{theorem}

\begin{proof}
Compare the $61$ classes of Proposition~\ref{prop:classes} at two states
that differ only in a dead player's TTD. Success classes never read TTD and
preserve both profiles' aliveness classification by
Lemma~\ref{lem:absorbing}, so they map to $\varphi$-identified children.
The failure class reads the checker's TTD only through $p$: if the checker
profile is dead, $p\equiv0$ and $F=+1$ with no child at all; if alive, the
TTD is not quotiented and $F$ is read off unchanged. Induction over the
finite acyclic graph (Corollary~\ref{cor:finite}) closes the argument.
\end{proof}

\begin{proposition}[Counting]\label{prop:count}
Over the transition-closed TTD domain $\{0\}\cup[60,300]$ (Lemma
\ref{lem:closure}), a player has $300\times242=72{,}600$ profiles;
$\varphi$ collapses them to $16{,}711$ alive profiles plus $300$ dead
sentinels (one per load), i.e.\ $17{,}011$ classes per player and
\[
17{,}011^2=289{,}374{,}121
\]
two-player classes: a factor $18.2$ below the $72{,}600^2\approx
5.27\times10^{9}$ reachable states. The raw coordinate envelope
$300\cdot301\cdot300\cdot301\approx8.15\times10^{9}$, on which the
previously recorded closure projection was priced, also counts alive TTDs
in $1..59$ that Lemma~\ref{lem:closure} rules out; against that envelope
the combined restriction and quotient is a factor $28.2$.
\end{proposition}

\begin{lemma}[Domain closure]\label{lem:closure}
Every root of interest has both TTDs in $\{0\}\cup[60,300]$, and the domain
is transition closed: revival requires $s_c+t_c\le240$, so a failure
child's TTD $t_c+s_c+60$ lies in $[60,300]$, and success preserves TTDs. A
live state with an \emph{alive} TTD in $1..59$ is thus unreachable from the
domain; the implementation rejects such states rather than approximate
them. A \emph{dead} TTD is discarded by $\varphi$ exactly, so any value is
accepted there.
\end{lemma}

Figure~\ref{fig:quotient} shows where the factor lives. Death eligibility
sounds like a rare endgame condition, but alive profiles occupy only $23\%$
of the full profile rectangle. Applied independently to both players, the
quotient leaves only $5.3\%$ of unquotiented state pairs entirely untouched.
The survival condition, zero plateau, absorption, and counts were checked by
exhaustive enumeration over all $72{,}600$ profiles.

\begin{figure}[t]
\centering
\begin{tikzpicture}[x=0.86cm, y=0.86cm]
% One-player profile geometry: the alive region is triangular, while every
% death-eligible vertical fibre collapses to one sentinel for its load.
\fill[figlightgray] (0,0) rectangle (4.983,5);
\fill[figpaleteal] (0,0) -- (4,0) -- (0,4) -- cycle;
\draw[figboxgray, line width=0.5pt] (0,0) rectangle (4.983,5);
\draw[figteal, line width=0.7pt] (4,0) -- (0,4);
\node[font=\scriptsize, rotate=-45] at (2.45,2.05) {$s+t=240$};
\node[font=\scriptsize, text=figteal!70!black] at (1.2,1.1)
  {alive $23\%$};
\node[font=\scriptsize, align=center, text=figink!70] at (3.35,4.1)
  {death-eligible\\$77\%$};
\fill[figpalepurple] (0,-1.1) rectangle (4.983,-0.72);
\draw[figpurple, line width=0.6pt]
  (0,-1.1) rectangle (4.983,-0.72);
\draw[-{Stealth[length=1.8mm]}, figorange, line width=0.8pt]
  (4.65,3.2) -- (4.65,-0.6);
\node[font=\scriptsize, text=figpurple, anchor=north] at (2.49,-1.18)
  {$300$ sentinels, one per load};
\node[font=\scriptsize, text=black!55, anchor=north] at (0,-0.08) {$0$};
\node[font=\scriptsize, text=black!55, anchor=north] at (4,-0.08) {$240$};
\node[font=\scriptsize, text=black!55, anchor=east] at (-0.08,4) {$240$};
\node[font=\scriptsize, text=black!55, anchor=east] at (-0.08,5) {$300$};
\node[font=\small, anchor=west] at (5.1,0.25) {load $s$};
\node[font=\small, rotate=90] at (-0.85,2.5) {time dead $t$};
\node[font=\small, align=center] at (2.5,6.0)
  {one player:\\ $300\times242$ profiles};

% Two-player product geometry: the small square is the only region on which
% neither player's profile is quotiented.
\begin{scope}[xshift=8.6cm]
\fill[figlightgray] (0,0) rectangle (3.7,3.7);
\fill[figpaleteal] (0,0) rectangle (0.85,0.85);
\draw[figboxgray, line width=0.5pt] (0,0) rectangle (3.7,3.7);
\draw[figteal, line width=0.7pt] (0,0) rectangle (0.85,0.85);
\node[font=\scriptsize, align=center, text=figink!70] at (2.25,2.35)
  {$\geq 1$ player dead\\$94.7\%$};
\draw[figteal, line width=0.6pt] (0.85,0.425) -- (1.15,0.425);
\node[font=\scriptsize, align=left, text=figteal!70!black, anchor=west]
  at (1.22,0.425) {both alive\\$5.3\%$};
\node[font=\small, align=center] at (1.85,4.35)
  {two players:\\ pairs of profiles};
\end{scope}
\end{tikzpicture}
\caption{The geometry of the quotient. Left: the alive triangle contains
$16{,}711$ of $72{,}600$ profiles; at each load, all dead TTD values map to
one of $300$ sentinels, leaving $17{,}011$ classes. Right: applying the map
to both players reduces $5.27\times10^9$ states to $289{,}374{,}121$, a
factor of $18.2$.}
\label{fig:quotient}
\end{figure}

\section{The potential and the sweep schedule}\label{sec:potential}

\begin{definition}[Potential]\label{def:potential}
For a profile let $\rho(s,t)=t$ if $(s,t)$ is alive and $\rho(s,t)=301$
otherwise; for a class let
\[
\Phi(x)=s_c+s_d+\rho(s_c,t_c)+\rho(s_d,t_d)\in\{0,\dots,1200\}.
\]
\end{definition}

\begin{theorem}[Strict increase]\label{thm:potential}
Every live transition strictly increases $\Phi$. By cases on the moving
profile:
\begin{itemize}[leftmargin=*]
\item successful lag $\ell$, profile stays alive or stays dead:
  $\Delta\Phi=\ell\ge1$;
\item successful lag $\ell$, alive profile dies:
  $\Delta\Phi=\ell+301-t\ge\ell+61$, since alive implies $t\le240$
  (and a dead profile never revives, so the reverse case is empty);
\item failure, revived profile alive: $\Delta\Phi=60$ (the load resets to
  zero while TTD absorbs the dose);
\item failure, revived profile dead: $\Delta\Phi=301-(s_c+t_c)\ge61$ by
  \eqref{eq:survive}.
\end{itemize}
\end{theorem}

\begin{corollary}[Layered backward induction]\label{cor:layers}
No transition stays within a $\Phi$-layer, so a descending sweep solves every
child before its parents. Any layer-$P$ residual work need only complete
before layer $P-1$ begins. The
$289{,}374{,}121$ classes occupy all $1{,}201$ layers; the largest,
$\Phi=374$, holds $1{,}678{,}715$ classes. A layer is a union of bucket
rectangles: grouping profiles by their own potential $s+\rho\in[0,600]$,
layer $P$ is exactly $\bigcup_{a}\,\text{bucket}(a)\times\text{bucket}(P-a)$.
\end{corollary}

The strict increase was checked over all $1{,}035{,}541$ live profile
transitions. The schedule needs no reachability analysis, frontier queue, or
transposition table; it simply solves unreachable classes as well.

\section{Certified equilibria}\label{sec:certified}

\begin{definition}[Certificate]\label{def:certificate}
For a stage matrix $M$ and mixed strategies $\sigma_d$ (Dropper) and $\sigma_c$
(Checker), let
\[
L=\min_c(\sigma_d^{\mathsf T}M)_c,\qquad
U=\max_d(M\sigma_c)_d,\qquad
\gap_M(\sigma_d, \sigma_c)=\max(0,\,U-L).
\]
The pair \emph{certifies} the value $\widehat v=(L+U)/2$ at tolerance
$\tol$ when $\gap_M(\sigma_d,\sigma_c)\le\tol$. Throughout, $\tol=10^{-6}$, and
the gap is always measured against the \emph{full} matrix
\eqref{eq:toeplitz}.
\end{definition}

\begin{proposition}[Certified midpoint]\label{prop:midpoint}
$L\le\val(M)\le U$, so a certificate at tolerance $\tol$ pins
$|\widehat v-\val(M)|\le\tol/2$.
\end{proposition}

\begin{proof}
$\sigma_d$ guarantees the row player at least $L$ against every reply, and
$\sigma_c$ caps the row player at $U$; $\val(M)$ sits between the two
levels.
\end{proof}

\begin{lemma}[Matrix-value Lipschitz bound]\label{lem:lipschitz}
If $\|M-\widehat M\|_\infty\le\epsilon$ entrywise, then
$|\val(M)-\val(\widehat M)|\le\epsilon$, since
$|\sigma_d^{\mathsf T}(M-\widehat M)\sigma_c|\le\epsilon$ for all mixed pairs.
\end{lemma}

\subsection{The solver ladder}

Each class is routed through a fixed ladder of certified solvers; the
certificate, not the solver, is the acceptance test, so a rung may fail
closed for any reason without affecting soundness.

\begin{enumerate}[leftmargin=*]
\item \textbf{Pure saddle.} Lemma~\ref{lem:toeplitz} computes the maximin
  and minimax of pure strategies in $O(60)$; if they meet within $\tol$
  the midpoint is certified by the corresponding pure pair.
\item \textbf{Equalizer supports.} Let $D$ be a guessed set of Dropper
  actions and $C$ a guessed set of Checker actions. Trim both to the same
  size $k=\min(|D|,|C|)$, then solve
  \[
  \begin{pmatrix}M_{D,C}&-\mathbf 1\\ \mathbf 1^{\mathsf T}&0\end{pmatrix}
  \begin{pmatrix}\sigma_{c,C}\\ u_c\end{pmatrix}
  =\begin{pmatrix}\mathbf 0\\ 1\end{pmatrix},
  \qquad
  \begin{pmatrix}M_{D,C}^{\mathsf T}&-\mathbf 1\\
  \mathbf 1^{\mathsf T}&0\end{pmatrix}
  \begin{pmatrix}\sigma_{d,D}\\ u_d\end{pmatrix}
  =\begin{pmatrix}\mathbf 0\\ 1\end{pmatrix},
  \]
  The first system chooses the Checker's mix so every supported Dropper
  action earns the same candidate value $u_c$; the transpose chooses the
  Dropper's mix so every supported Checker action yields the same candidate
  value $u_d$. For a valid support these values agree. Reject on singularity
  or negative mass, and certify the lifted pair
  against the full matrix in $O(60k)$ via \eqref{eq:toeplitz}. Guessed
  supports come from spatial neighbours one potential step up (the
  lag-one shift of either profile), recorded when the neighbour was
  solved. The case $k=60$ is the classical full-support structured solve
  and needs no guess. This case matters: $99.8\%$ of the game's equilibria
  put mass on all $60$ actions.
\item \textbf{Linear programming.} The standard LP for the row
  player with the column strategy extracted from its inequality duals,
  falling back to two independent LPs, and finally to the same two LPs
  under tightened solver feasibility tolerances ($10^{-10}$, dual
  simplex). The retry tightens the \emph{solver}, never the gate: every
  answer is certified at $\tol$ against the full matrix, and a matrix
  failing all rungs aborts the computation rather than store an
  uncertified value.
\end{enumerate}

\subsection{Propagated error}

The table stores certified midpoints, so a parent's matrix is assembled
from child values that are themselves within a bounded distance of truth.

\begin{proposition}[A priori accumulated bound]\label{prop:accumulated}
Let every stored value carry a certificate at tolerance $\tol$ and let
$\delta(x)$ bound the longest live path from $x$ to a terminal. Then the
stored value $\widehat V(x)$ satisfies
\[
|\widehat V(x)-V(x)|\;\le\;\delta(x)\,\frac{\tol}{2}
\;\le\;\frac{1200-\Phi(x)}{2}\,\tol .
\]
In particular the initial state obeys
$|\widehat V-V|(0,0,0,0)\le6.0\times10^{-4}$.
\end{proposition}

\begin{proof}
Induction up the DAG. Terminal entries are exact. If every child satisfies
the bound with $\delta(x)-1$, the assembled matrix is entrywise within
$(\delta(x)-1)\tol/2$ of the true one (expectations and negation are
non-expansive), Lemma~\ref{lem:lipschitz} transfers that to the value, and
Proposition~\ref{prop:midpoint} adds the state's own $\tol/2$. Each live
transition raises $\Phi$ by at least one (Theorem~\ref{thm:potential}),
bounding $\delta$.
\end{proof}

This a priori bound is loose by design; Section~\ref{sec:computation}
reports the measured gaps, which are typically at the $10^{-12}$ scale and
never exceeded $4.9\times10^{-7}$ on any audited sample. Every stored
value's certificate is re-derivable on demand from the stored children
(Bellman recertification), so no per-class certificate is persisted.

\section{The computation}\label{sec:computation}

\subsection{Sweep schedule}

Classes are addressed as $p_c\cdot17{,}011+p_d$ and solved one potential
layer at a time, with checkpoints between layers. Revival probabilities
\eqref{eq:revival} are evaluated once; the numerical kernels then gather
from those values and perform no transcendental arithmetic.

\subsection{Cross-implementation audit}

The sweep is implemented twice: a Python reference that defines the
semantics, and a parallel Rust kernel constrained to \emph{bit-identical}
arithmetic: pinned elimination order, first-maximum pivoting, no fused
multiply-add, sequential ascending reductions, and per-state warm-start
guesses precomputed so that no kernel result depends on thread count or
work partition. On every region computed by both (all synthetic
end-to-end sweeps, and a forked continuation of the production run)
the two implementations produced byte-identical value and routing arrays.
The LP residue rung is shared
Python code by construction. This does not make the two implementations
fully independent, but it shows that the Rust path matches the reference
where compared. The complete table was produced once, through the Rust
kernel, and every accepted value passed the same certificate gate.

\subsection{Results}

The full sweep over all $289{,}374{,}121$ classes completed in $5{,}432$
seconds ($1.51$ hours) on a single
twelve-core consumer desktop (AMD Ryzen~9 3900X), against the previously
recorded single-core projection of $3.7$--$5.1$ years for the unquotiented,
search-based design. Solver routing:

\begin{center}
\begin{tabular}{@{}lrr@{}}
\toprule
rung & classes & share\\
\midrule
pure saddle (Lemma~\ref{lem:toeplitz}) & $334{,}588$ & $0.116\%$\\
equalizer, warm-started support & $38{,}138$ & $0.013\%$\\
equalizer, full support ($k=60$) & $288{,}810{,}400$ & $99.805\%$\\
linear programming & $190{,}995$ & $0.066\%$\\
\quad of which dual-extraction single LP & $190{,}986$ & \\
\quad of which tightened-tolerance retry & $9$ & \\
\bottomrule
\end{tabular}
\end{center}

\noindent
Complete-game DTH equilibria are overwhelmingly full-support. This reverses
the small-support pattern previously measured on shallow finite-horizon
matrices and makes the classical structured solve the workhorse.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{axis}[
  width=0.95\textwidth, height=5.8cm,
  xlabel={second}, ylabel={probability},
  xmin=0, xmax=61, ymin=0.005, ymax=0.35,
  ymode=log,
  log ticks with fixed point,
  ytick={0.005,0.01,0.03,0.1,0.3},
  yticklabel style={/pgf/number format/fixed},
  legend style={at={(0.5,0.97)}, anchor=north, legend columns=2},
]
\addplot[figblue, line width=0.8pt, mark=*, mark size=1pt]
  table[x=action, y=drop] {build/figures/root_strategies.dat};
\addplot[figorange, dashed, line width=0.8pt, mark=*, mark size=1pt]
  table[x=action, y=check] {build/figures/root_strategies.dat};
\legend{Dropper $\sigma_d$, Checker $\sigma_c$}
\end{axis}
\end{tikzpicture}
\caption{The certified equilibrium at the initial state $(0,0,0,0)$,
recertified at gap $1.4\times10^{-15}$. Both strategies are full-support.
The Dropper concentrates $27.3\%$ on dropping immediately and shades the
remainder toward later seconds; the Checker mirrors it. The logarithmic
axis keeps the atom and the remaining support visible together.}
\label{fig:root}
\end{figure}

\begin{figure}[tbp]
\centering
\includegraphics[width=0.96\textwidth]{build/figures/seaborn/fig5_strategy_and_value.pdf}
\caption{Two zero-TTD slices, sampled in $10$-load steps. \textbf{(a)} The
Dropper's equilibrium mix along $(s,0,s,0)$. Every sampled strategy has full
support; the survival boundary at $s=240$ produces atoms on seconds $1$ and
$60$, before the mix flattens near overflow. \textbf{(b)} Certified values
$\widehat V(s_c,0,s_d,0)$. At equal loads of $290$, the mover's value is
$0.753$; unequal high loads create the opposing cliffs.}
\label{fig:surfaces}
\end{figure}

Audits on the finished table include a completeness scan (every value finite,
in $[-1,1]$, with a known routing byte); Bellman recertification of
$4{,}792$ deterministically sampled classes with worst deviation
$4.84\times10^{-7}$; agreement with the independently produced endgame
band tablebase (both loads ${}\ge240$) on
all $3{,}541$ shared classes to at most $4.9\times10^{-8}$, and at its
root to $6.9\times10^{-12}$; and an independent dead-band reference solver
sharing no sweep machinery.

\begin{theorem}[Main result]\label{thm:main}
The complete Drop-the-Handkerchief game of Section~\ref{sec:game},
restricted to the transition-closed quotient domain of
Lemma~\ref{lem:closure}, is solved: every one of the $289{,}374{,}121$
classes carries a stored value certified at saddle-gap tolerance
$10^{-6}$, with the a priori accumulated bound of
Proposition~\ref{prop:accumulated} and the measured audits above. At the
initial state,
\[
\widehat V(0,0,0,0)=0.0898500728095116,
\]
Thus under optimal play the first Dropper wins with probability
$\tfrac{1+\widehat V}{2}\approx0.5449$, within the stated error bounds;
equilibrium strategies are re-derived from stored child values on demand.
\end{theorem}

\section{Scope of the claims}\label{sec:claims}

\begin{itemize}[leftmargin=*]
\item \textbf{Rules-relative.} The theorems are about the game defined in
  Section~\ref{sec:game} with the frozen revival surface
  \eqref{eq:revival}. Nothing here solves the leap-aware ``Surpassing the
  Leader'' game, which adds an asymmetric 61st second and is a different
  game.
\item \textbf{Certified numerics, not rational arithmetic.} Values are
  IEEE double midpoints with explicit saddle-gap certificates and the
  accumulated bound of Proposition~\ref{prop:accumulated}; this is a
  certified-numerics result, not an exact rational or interval-arithmetic
  proof. The structural theorems (quotient, potential, finiteness,
  Toeplitz form) are exact and machine-checked exhaustively.
\item \textbf{Domain.} The table covers the transition-closed quotient
  domain; live states with an alive TTD in $1..59$ are outside it and are
  rejected, not approximated (Lemma~\ref{lem:closure}).
\item \textbf{Values, not tabulated policies.} The table stores values and
  routing only; policies are re-derived and re-certified per query, as in
  Figures~\ref{fig:root} and~\ref{fig:surfaces}.
\end{itemize}

\section*{Reproducibility}

From the repository head: \texttt{uv run python -m dth complete} builds the
artifact (checkpointed, resumable; the configuration is
\texttt{src/dth/config/complete\_full\_v1.yaml}); the exhaustive structural
tests and both-backend parity suite run under
\texttt{uv run python -m pytest src/dth/tests -q} and
\texttt{cargo test --workspace}. The artifact manifest embeds SHA-256
digests of every array and of the solver sources that produced it.

\vspace{-0.8em}
\begin{thebibliography}{9}
\footnotesize
\setlength{\itemsep}{0pt}

\bibitem{stlevidence}
Toshio Sako. \emph{Usogui}. Shueisha, 2006--2018.
\newblock Source evidence indexed in \emph{Canonical game evidence}, project
ledger entries E-CYLINDER-CAP, E-DOSE-COMPOSITION, E-PROXIMITY-RISK,
E-REFEREE-CONDITION, and E-TTD-SLACK, 2026.
\newblock \href{https://youngjump.jp/manga/usogui/}{Official series page.}

\bibitem{zermelo1913}
Ernst Zermelo.
\newblock \"Uber eine Anwendung der Mengenlehre auf die Theorie des
Schachspiels.
\newblock In \emph{Proceedings of the Fifth International Congress of
Mathematicians}, volume 2, pages 501--504, Cambridge, 1913.

\bibitem{vonneumann1928}
John von Neumann.
\newblock Zur Theorie der Gesellschaftsspiele.
\newblock \emph{Mathematische Annalen}, 100:295--320, 1928.
\newblock \href{https://doi.org/10.1007/BF01448847}{doi:10.1007/BF01448847}

\bibitem{shapley1953}
Lloyd S. Shapley.
\newblock Stochastic games.
\newblock \emph{Proceedings of the National Academy of Sciences},
39(10):1095--1100, 1953.
\newblock \href{https://doi.org/10.1073/pnas.39.10.1095}%
{doi:10.1073/pnas.39.10.1095}

\end{thebibliography}

\end{document}
