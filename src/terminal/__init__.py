"""The terminal STL game: ``python -m terminal play``.

The app reads the human's seconds from the terminal and draws the game as plain
text or as the ANSI scene in :mod:`terminal.tui`. It takes the game core and the
Hal providers from :mod:`arena`.
"""
