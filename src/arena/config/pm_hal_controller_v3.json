{
  "schema_version": "arena-pm-hal-controller-config-v3",
  "status": "git-registered-before-v3-confirmation",
  "config": {
    "action_count": 60,
    "adaptive_prior_strength": 0.25,
    "adaptive_decay": 0.70,
    "fixed_share": 0.04,
    "source_learning_rate": 1.0,
    "controller_learning_rate": 2.0,
    "controller_prior_scale": 0.002,
    "metric_retention": 0.92,
    "log_probability_floor": 1e-9,
    "bocpd_hazard": 0.08,
    "bocpd_prior_strength": 0.50,
    "bocpd_max_run_length": 256,
    "outcome_prior_strength": 0.25,
    "evidence_strength": 4.0,
    "maximum_confidence": 1.0,
    "minimum_role_observations": 1,
    "change_shield_threshold": 0.35,
    "press_confidence_threshold": 0.15,
    "dominate_confidence_threshold": 0.32,
    "probe_disagreement_threshold": 0.40,
    "dominate_disagreement_threshold": 0.18,
    "dominate_change_threshold": 0.10,
    "epsilon_grid": [0.0, 0.01, 0.02, 0.05, 0.10, 0.20, 0.50, 1.0, 2.0],
    "game_epsilon_budget": 12.0,
    "probe_epsilon_cap": 0.05,
    "press_epsilon_cap": 0.50,
    "dominate_epsilon_cap": 2.0,
    "posterior_samples": 256,
    "posterior_meta_concentration": 6.0,
    "minimum_improvement_support": 0.55,
    "improvement_tolerance": 1e-10,
    "response_temperature": 0.0,
    "best_response_tolerance": 1e-12,
    "perfect_config": {
      "action_count": 60,
      "prior_strength": 0.02,
      "conditional_prior_strength": 0.10,
      "recency_retentions": [0.995, 0.82, 0.30],
      "expert_learning_rate": 1.25,
      "expert_weight_retention": 0.92,
      "expert_weight_floor": 0.002,
      "log_probability_floor": 1e-9,
      "periodicities": [2, 3, 4, 5, 6, 8],
      "response_temperature": 0.0,
      "best_response_tolerance": 1e-12
    }
  },
  "notes": [
    "The numeric aggression policy is unchanged from v2; v1 and v2 are development evidence.",
    "V3 fixes global public-outcome context, identity-seeded Monte Carlo, and shared candidate perturbations before confirmation.",
    "Improvement support is a heuristic Monte Carlo controller score, not a calibrated probability.",
    "Local matrix loss remains exact; the cumulative charge is a risk policy, not a whole-game safety theorem."
  ]
}
