defaults:
  - command: none
  - experiment: smoke
  - _self_

hydra:
  job:
    chdir: false
  run:
    dir: src/stl/outputs/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: src/stl/outputs/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
