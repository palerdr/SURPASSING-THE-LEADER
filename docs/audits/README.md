# `docs/audits/`

Generated audit traces and counterfactual outputs.

These are useful when investigating a specific run, but they are not required
for normal development or for `scripts/run_tier_a.py`.
