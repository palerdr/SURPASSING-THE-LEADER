# `docs/whitepaper/`

STL solver whitepaper:

- `stl_solver_whitepaper.tex` - source.
- `stl_solver_whitepaper.pdf` - rendered reference copy.

Temporary build logs belong in `docs/whitepaper/build/`, which is gitignored.

The source distinguishes implemented exact/search machinery from the planned
RL loop. Rebuild the checked-in PDF from this directory with a modern LaTeX
engine such as Tectonic, then render every page for visual review.
